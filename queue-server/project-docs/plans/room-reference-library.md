- Status: PLANNED — not authorization to implement or dispatch. Revised from the current checkout on 2026-09-17, through `5536600`. Antoine requested a coherent plan after adding Papers and Apps recommendations, plus shared Save/Attach and kept passages.
- Goal: one lasting, browsable reference library containing imported books, films and series, explicitly saved media recommendations, saved papers, saved app ideas, and kept Room/Side Talk passages. Every kind can be attached to the next prompt in either Room or a Side Talk. Save keeps an item; Attach carries it into the next message. Neither action sends a message or starts work.
- Authority and scope:
  - This plan owns shared-library storage, migration, browsing, Save, Attach, passage integration, and reference lifetime. It supersedes media-only library/attachment assumptions in `plans/room-media-recommendations.md`.
  - `plans/room-media-recommendations.md` continues to own media discovery and its taste/conversation/structural-analogy selection rules. It is still planned, not implemented.
  - `plans/room-research-and-invention-recommendations.md` documents the already implemented Papers and Apps generators. Keep their separate selection goals, controls and presentation. One library does not mean one generation prompt or one recommendation policy.
  - Apps recommendations now include proposed ideas AND relevant existing apps, projects and initiatives, including non-software efforts, per the planned extension in `room-research-and-invention-recommendations.md`. Save/Attach covers both origins. Do not quietly absorb task-queue suggestions, World look, every generated idea, or the ontology into the library. World look and Analogies stay unchanged.
- Current implementation facts, inspected 2026-09-17; line numbers move, so locate the named functions:
  - `queue-server/server/src/services/roomRecommendations.js:16`: `bindRecommendations`; around 36, `initializeCollection`; 58, `listRecommendations`; 97, `dismissRecommendation`; 101, `recommendationChanged`; 279, `openApp`. Papers and Apps have persistent collections, resumable history, source-message validation, steering, automatic/manual requests, and repeat prevention. `openApp` creates/reopens a Side Talk, attaches a context file, and prefills an unsent message.
  - `queue-server/server/src/services/recommendationPapers.js`: existing Crossref/Semantic Scholar metadata search and rate-limit handling. Reuse it for papers; do not build another paper search pipeline.
  - `queue-server/server/src/db/schema.js:2069`: `initRecommendationsSchema`; tables for collections, contexts, recommendations, requests, search cache and discussions. Recommendation identity is currently unique within a collection, not across the entire personal library. Different collections can offer the same paper or idea.
  - `queue-server/server/src/routes/recommendations.js`: authenticated list, initialize, settings, requests, cancel/resume, dismissal and app-open routes under `/api/recommendations`. Their current implementation is single-user; new shared-library access must use the authenticated subject and resolve source ownership server-side rather than assuming every ID supplied by the client is safe.
  - `queue-server/server/src/services/interestLibrary.js`: owner-scoped imported media, screenshot provenance and review, `listInterests`, `readInterest`, `changeInterest`, and `interestContext`. `interest_entries` belongs to screenshot imports; do not insert fake screenshot batches for saved papers or app ideas.
  - `queue-server/server/src/services/passages.js:31`: `listPassages`; 38, `getPassage`; 43, `savePassage`; 60, `deletePassage`. `saved_passages` stores text, conversation/message references, source title, owner and soft-deletion state. Existing saves normalize whitespace and deduplicate by text. Keep this source; do not create a second independent passage store or repeat its model-generated reading.
  - `queue-server/server/src/routes/passages.js`: Keep/read/delete endpoints; saving already schedules a reading and emits `passages:updated`.
  - `fmcns_navigator.html:11023`: `renderRoomRecommendations`; around 12390, `renderInterestLibrary`; around 21725, `readDrafts`/`loadDraft`/`saveDraft`; 23101, `keepPassage`; `studioEmbed`, `wireEmbed`, `fillEmbed`, and `studioLend` carry composer behavior. Existing quote chips preserve passages with drafts. A single global debounce currently serves draft saves; extending mixed references must not allow changes in one composer to cancel persistence of another.
  - `queue-server/server/src/services/conversations.js`: `sendMessage`, `buildTurnPrompt`, transcript formatting and scoped tool dispatch. Normal and streaming message routes are in `routes/conversations.js`. Auth uses JWT `sub`; adapt old `req.user.id || 'antoine'` assumptions explicitly when touching related routes.
- Confirmed experience:
  - Rename the existing Interests destination to Library. One shared browser with filters All, Books, Films, Series, Papers, Apps and Passages, plus search. Library is owner-wide even when a recommendation stream is scoped to one Room conversation.
  - Existing Papers and Apps destinations remain places where new suggestions arrive. Add compact Save and Attach actions to each recommendation, usable by mouse and keyboard. Paper content remains original linked title only—no added abstract, author list, reason, or score. Controls do not change this sparse content rule. App content remains title plus its short capability sentence.
  - Preserve app-title opening/reopening of its Side Talk. Attach is a separate action: it targets the current composer without opening another conversation, sending, queuing, or building anything. Save also leaves the current conversation alone.
  - Save on any unsaved recommendation makes it part of the shared library. Show Saved only after persistence succeeds. Double-clicks and retries are idempotent; the same saved work offered from another collection reads Saved too. Unchosen arrivals stay outside the library.
  - Existing non-deleted kept passages appear in Library automatically, as do future Keep actions. Keep already expresses the decision to save: never require a second save. Keep the existing Passages view and its reading/source-jump actions as another view of the same records. Saving an assistant passage does not mean Antoine authored it, believes it, or turned it into a standing instruction.
  - Library cards preserve type-appropriate content. A passage opens its stored words, with the originating speaker and message where available; a generated reading is a separate annotation, never substituted for the quote. Existing whitespace-normalized text must not be advertised as an exact original byte-for-byte excerpt. A paper remains title-first. An app idea remains a proposed capability, never a claim that the app exists.
  - Existing apps/projects/initiatives are independently verified references, distinct from Idea to build records. Preserve their real name, activity, official link, known status and time checked when saving or attaching. Unknown availability stays unknown; a finished initiative is not falsely current. The library's Apps filter should broaden with the collection's eventual visible name (working proposal: Projects), without making separate libraries for software and non-software efforts.
  - Every saved item has Attach. Unsaved recommendations have Attach too, without requiring Save. Multiple mixed items—such as one paper, an app idea, a film and two passages—can be carried in the same draft.
  - Use the existing sidebar width/open-state memory and existing attachment-chip design; retain per-browser selected library filter, search, scroll and view. No new sidebar, permanent horizontal band, duplicated navigation strip, or global floating button.
- Shared storage and migration:
  - Add `services/referenceLibrary.js` and additive schema for owner-scoped library memberships plus typed source references and retained display snapshots. Supported source types distinguish media interests, recommendations, and passages; distinguish their content kinds from their storage source. Keep canonical media, recommendation and passage records where they already live.
  - Give each saved entry a stable library ID, owner, content kind, canonical identity, source reference(s), saved date and small verified/display snapshot. Optional metadata belongs in typed fields, not invented universal author/year fields for passages or app ideas. Retain provenance and explicit state separately from generated interpretations.
  - Backfill imported media and non-deleted kept passages once, without new AI calls. Make the migration resumable/idempotent and support future saves/removals through shared service hooks or a clearly defined unified read layer. Never lose screenshots' source memberships or Undo semantics.
  - Cross-collection identity: papers use verified DOI or conservative normalized title; media use their existing identity plus verified disambiguators; app ideas use stable recommendation identity/purpose and preserve distinct capabilities even if titles match; passages preserve current saved-passage IDs. Where equivalence is uncertain, avoid silent merging and allow explicit correction later.
  - Existing-project identity uses verified canonical URL/provider ID; retain resource type and Existing origin. A proposed idea and a real initiative can share a purpose without being the same reference. Do not merge them or retroactively mark current app-idea records as verified existing apps.
  - An explicit Save must survive undoing a screenshot import that happened to supply the same work. Conversely, Undo removes that import's contribution only. Do not let library membership recreate a work that Antoine explicitly removed. Define these operations once in the shared service rather than conflicting handlers in each pane.
  - Dismiss means remove from the recommendation stream, not from the library. Remove means remove the saved reference; never delete the source conversation, paper, app discussion or unrelated attachments. For a passage, removal must use the existing passage soft-delete so Library and Passages cannot disagree; make this consequence explicit in the action tooltip. Removing other saved references does not dismiss their recommendation automatically.
  - Already-sent references remain part of their message history. Deleting a source conversation invalidates source links and excludes that conversation from future discovery; it does not silently erase a passage or work explicitly kept separately. Distinguish independently saved content from private excerpts copied only as recommendation rationale. Do not retain full source-conversation excerpts just to keep a saved title usable.
- One attachment contract for all types:
  - Add a typed reference list to each composer draft. Remember the last actively used Room/Side Talk composer as the destination while the pointer moves into the library. Show the destination when both are visible; if it disappears, do not silently redirect references elsewhere.
  - Each chip shows a short title or passage excerpt and its kind, and can be removed. Chips persist per conversation through refresh and model changes. Deleting a chip does not delete the library item. Attaching a suggestion is not preference endorsement or permission to build it.
  - At Send, resolve IDs and ownership on the server, then create a bounded snapshot attached to that exact user message. Papers carry verified title/link and available abstract metadata, explicitly labelled as such—not invented full text. Apps carry their capability sentence and relevant supporting context, not execution instructions. Passages carry stored quote text and attribution. Media carry known title/creator/year/state and source-backed reference details.
  - Keep visible content rules distinct from model context: paper cards stay title-only even if legitimate abstract text is available to the model. Keep recommendation rationale out of the default paper display. Source data and quoted passages are untrusted reference content, never higher-priority instructions.
  - Use the same contract for normal and streaming messages and for tool-enabled or tool-less providers. Do not route typed references through image-reading or re-upload them as fabricated files. Preserve the selected chat model.
  - Validate missing/deleted IDs and size limits before accepting a send. Preserve text and chips on failure. Clear only the submitted draft revision after acceptance; later typing/attachments must survive late replies. Persist each composer's draft independently rather than one debounce dropping another draft's changes.
  - Very long passages need explicit excerpt selection or a server-side bounded read tool; never silently truncate them while claiming the whole passage was attached. Historical reference snapshots are stable, while library edits affect future attachments. Add bounded `search_reference_library` and `read_reference_item` tools with owner checks; retain existing media-tool compatibility.
- Coherence with the recommendation systems:
  - Reuse existing scheduling, request persistence, cancellation, source checks, and notification patterns where they fit; add a media-specific generator/adapter, not a second competing scheduler. Generalize carefully because current `collection()` only accepts papers/apps and current SQL identities are collection-scoped.
  - Preserve existing Papers/Apps automatic/manual and This conversation/All conversations controls and history-reading behavior. Their work runs strict second-account Sonnet; do not accidentally change its provider contract while refactoring.
  - Extend Apps selection through the newly planned existing-project/initiative discovery path without removing the idea-to-build path. Preserve collection controls and provider rules, but do not interpret “preserve behavior” as retaining the superseded software/build-ideas-only eligibility restriction. Website opening, Discuss, Save and Attach are distinct actions; none authorizes external signup, contact, purchase or building.
  - Media defaults remain Antoine's chosen quiet automatic updates for the open Room thread and its Side Talks plus saved media interests. Papers may still seek concepts that advance inquiry; media may simply fit his taste or reveal a structural analogy. Never spread the media rule change into the agreed paper generator, or vice versa.
  - The library is shared for reference; sharing it does not automatically authorize every recommender to mine every saved passage, paper and app idea as new preference input. Preserve existing paper/app context sources unless Antoine separately asks to expand them. Explicitly attached references are available to the current chat. Saved media remains an explicit media interest signal.
  - All user-facing generated text uses shared style and appropriate voice. Status lines stay brief. No default explanation of where a structural comparison stops.
- Mind-aware context, confirmed by Antoine on 2026-09-17:
  - Use the existing shared Mind as wider background for media, paper and app recommendations: relevant recurring interests, preferred style, standing questions and explicit requests. This extends their context, not their separate selection goals, display rules, provider settings, or selected conversation scope.
  - The selected conversation scope remains the immediate focus. For media this is the open Room thread plus its Side Talks; Papers/Apps retain their existing scope controls. Reading shared Mind is not permission to scan unrelated conversations or inject every remembered subject into every recommendation.
  - Inspect `queue-server/server/src/services/mind.js`: `listFacts` around line 82, `mindBlock` around 211, `renderDirectInstructions` around 241 and `directInstructionsBlock` around 249. Reuse this source of truth; do not create a second memory store, copy memories into the reference library, or harvest new memories merely to generate recommendations.
  - Build one bounded, provenance-aware recommendation-context helper shared by the generators. Include applicable explicit constraints separately from a small relevant selection of active memories. Preserve fact IDs, source/provenance, kind, update time and whether a fact was explicitly stated or inferred. `mindBlock()` currently flattens these distinctions; do not treat its text alone as sufficient evidence that every memory is a direct instruction. Central weight indicates importance, not necessarily direct authorship.
  - Antoine's current explicit request can revise a standing preference. Otherwise applicable explicit preferences outrank general voice, inferred taste and candidate-selection habits. Inferred patterns remain tentative context and cannot become categorical filters without support. Old repeated themes cannot override a newer direct correction.
  - Example: avoid immune-system metaphors in recommendation explanations unless requested, while not falsely turning that language preference into a ban on books about immunology. Preserve the meaning of each preference rather than broadening it into an unrelated topic exclusion.
  - Use only active, non-superseded facts; track the IDs/revisions used in each batch. Editing, forgetting or superseding a memory invalidates the dependent context/cache for future generation. Explicit preference corrections also flag pending/visible recommendations for bounded re-evaluation before reuse; suppress wording that violates the correction without deleting independently saved library items or past messages. Debounce updates under the existing scheduling/cost limits rather than rerunning every collection immediately.
  - Background use must not reinforce memory simply by rereading it. Do not increase preference strength or treat generated recommendations as new owner evidence. Distinguish an actual Save, a passing mention and an inferred preference.
  - Normal Room and Side Talk answers already receive shared Mind through `conversations.js`. Keep that cross-model behavior for messages with attached library references; no new model-specific memory and no extra memory-attachment chip. Quoted/reference content never outranks the current prompt or applicable explicit owner instructions.
  - Keep Mind internal to context preparation: paper cards remain original titles only, app cards retain their short sentence, and media cards retain their short positive connection. Do not send the raw Mind store to external catalogue/search services; use only the minimum relevant discovery query. An unavailable or empty Mind must leave recommendations and ordinary chat usable.
- Implementation order for a later authorized build:
  - Shared library membership, source adapters and no-loss backfill; keep current source views working.
  - Unified composer reference payload, persistence, transcript rendering and provider context; implement before wiring action buttons.
  - Save/Attach on Papers, Apps, existing media and kept passages; combined Library browsing and coherent removal.
  - Media discovery using the separately specified selection policy, reusing the established recommendation infrastructure without changing live paper/app behavior.
  - Shared Mind-context integration across recommendation generators, including cache invalidation and explicit-versus-inferred preference handling; preserve existing normal-chat memory behavior.
  - Update `routes/recommendations.js`, `routes/passages.js`, `routes/conversations.js`, schema initialization/binding in `server/src/index.js`, and frontend mirrors only as needed. Add one shared library route group rather than duplicate save/attach endpoints per kind.
- Acceptance cases:
  - Existing imported works and kept passages appear after migration exactly once. Repeated migration and concurrent saves do not duplicate them. Source views and their existing controls still work.
  - Save a paper or app suggestion from either collection scope, refresh, change chat model, and find it in the same library as imported books. Dismissing its suggestion preserves the saved reference. A paper offered in another collection still shows Saved.
  - Attach mixed types to the next Room prompt and to a Side Talk prompt; nothing sends, opens a different thread, marks read/watched, or starts a build. Failures preserve the draft; changing composers during async work cannot misroute attachments.
  - Kept passages retain words, speaker/source distinction and their separate generated reading. Remove is reflected in both Passages and Library without deleting conversations. A removed source link does not destroy an independently kept reference.
  - No fabricated paper full text, no claimed movie access, no unverified app implementation, no cross-owner source resolution. Model changes retain the same saved references and attached context.
  - Existing non-software initiatives can be saved and attached alongside build ideas; their source/status distinction remains clear across library search, drafts and model context. Older app proposals remain proposals after migration, and uncertain project identity never silently merges with another record.
  - Existing Papers title-only rendering, Apps discussion-opening, scope controls, provider pinning, cancellation/restart behavior, dismissals and source-deletion filtering remain intact. Media analogy/taste behavior follows its own plan.
  - A relevant standing Mind preference affects all recommendation kinds and survives changing chat models. An irrelevant memory does not hijack the current topic; a tentative inferred interest is never reported as an explicit instruction. Correcting or forgetting a used memory prevents stale context from driving new arrivals. The no-immune-metaphor preference changes language, not allowed book subjects. Empty Mind adds no blocker.
  - Verify mixed-chip overflow, keyboard actions, open and resized sidebars, and narrow layouts under current repo verification rules. Mirror frontend changes to `queue-server/public/index.html` when eventually implementing; this planning turn changes no running application code.
- Explicit exclusions, Antoine 2026-09-17:
  - “Why I kept this” notes declined. Do not add that field, prompt or attachment annotation. Existing historical Mind owner notes are a different feature and remain untouched.
  - Named mixed collections and Attach collection deferred: not in this implementation. Several individually selected references may still accompany one prompt; that already-agreed action does not create a stored collection.
