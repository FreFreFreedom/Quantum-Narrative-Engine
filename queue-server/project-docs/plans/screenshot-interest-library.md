- Status: PLANNED, 2026-09-17. Antoine requested a plan, not implementation or queue dispatch. This is not a green light to build.
- Purpose: QNE's Room should remember the books, films, and TV series Antoine saves as interesting. His words: “i could just drag these on the chatroom in the app and its automatically adding them.” No Amazon or IMDb account connection, purchase, or manual transcription required.
- This replaces the first-release import approach in `plans/amazon-book-lists-in-the-room.md`. Keep its shared-interest-library intent, but start with screenshots, not Amazon HTML exports, list links, or a browser extension. Those remain optional later sources, not prerequisites.
- Scope: screenshot file drops in the Room and Side Talk composers, plus their existing image picker and paste paths where available. One owner-level library shared by all his conversations and models. Do not enable automatic library capture in unrelated embedded chats merely because they share the composer. Browser-tab or bookmark drags containing URLs rather than image files are a separate future input.
- Existing app: Room threads and Side Talks use the shared `studioEmbed` composer in `fmcns_navigator.html`. `queue-server/server/src/services/conversations.js` persists conversation messages and assembles model context. `queue-server/server/src/services/studioTools.js` exposes bounded read tools. The interest library itself is not implemented.
- Starting code locations, inspected 2026-09-17; line numbers will move, so find the named functions before editing:
  - `fmcns_navigator.html:22196`: `studioEmbed`; around 22406 the composer drop handler calls `addFiles`. Reuse the existing file intake instead of adding a competing drop listener.
  - `fmcns_navigator.html:10668` and `:11303`: Room and Side Talk mounts. Pass an explicit library-capture capability into these mounts only.
  - `fmcns_navigator.html:12187`: `roomUploadFileToConvo`; the file-text path is distinct from image messages. Do not send image bytes to a text-only extractor.
  - `queue-server/server/src/routes/conversations.js:515` and `:550`: normal and streaming message routes carry images. Around 584, `/:id/files` accepts already-extracted file text, not raw multipart uploads.
  - `queue-server/server/src/services/conversations.js:2677`: `sendMessage`; current image validation limits formats, count, and total data. Reuse or centralize these checks. Around 1710, `buildTurnPrompt` is the shared model-context entry point.
  - `queue-server/server/src/services/ai/text.js:743`: `generateText`; the image branch currently chooses a Google vision model even when a caller named another provider. Never route the user's ordinary answer through that branch merely to import a screenshot.
  - `queue-server/server/src/db/schema.js`: additive database setup. `services/books.js:35` has a best-effort Google Books lookup, but its first-result approach is not safe enough to decide ambiguous work identity automatically.
  - `queue-server/server/src/services/ai/style.js`: shared user-facing style. Every generated explanation or review question must include it and respect Antoine's latest bullet-only answer rules. Machine-readable extraction output must remain valid structured data, not prose.
- Interaction:
  - Dropping a screenshot starts a bounded background read immediately; no Send click or approval dialog required for clearly recognised list entries. Keep the composer and conversation usable throughout.
  - A screenshot of an Amazon book page, IMDb title, or saved-tabs list can contain several media types. Extract all visible entries, not just the highlighted row. Treat other images as ordinary attachments without adding anything to the library.
  - Recognised list screenshots receive a compact import chip in the composer or adjacent existing attachment area. Show progress, then actual added/duplicate/unresolved counts, with Undo and a review action. No long explanation, new toolbar band, modal veil, or unsolicited chat answer.
  - Example result for Antoine's latest screenshot if every visible entry is legible: “Added 10 books, 2 films, 2 series.” Compute this from persisted results; never hardcode the example.
  - Only uncertain rows wait for review; clear rows save immediately. Keep uncertain rows visible as unresolved, not fabricated matches. A failed read gets a short retryable error, never a false success.
  - Retain ordinary image-send capability. An import chip alone must not send the same image to the chat model again. If Antoine deliberately sends an image for discussion, preserve existing behavior and clearly distinguish that action from library capture.
  - Reopening a thread shows any pending import and its outcome. Navigating away, repainting, or receiving a late response must not attach the result to a different conversation.
- Durable storage and ownership:
  - Add library works, import batches, and import-entry/source records. Scope every read and write to the authenticated owner; validate access to the originating conversation and every batch/item ID. Never trust an owner ID supplied by the browser.
  - A work records kind (`book`, `film`, `series`), title, visible author/creator where known, year or series year range where visible, identifiers when actually available, and an editable interest state. Default is interested, never read/watched/bought/endorsed.
  - Sources record batch ID, origin conversation, screenshot hash, visible row text, extraction status, and any explicitly resolved metadata. Keep observed text separate from later enrichment and model interpretation. A URL or identifier cannot be reconstructed from a site's icon.
  - Persist a retryable job before processing. Use bounded image dimensions/bytes, MIME validation, request throttling, timeouts, and a concurrency cap. Process every accepted screenshot in a batch; report oversized or excess files explicitly rather than silently dropping them.
  - Use bounded, access-controlled temporary screenshot storage suitable for the existing deployment. Store it only as long as needed for processing/review; purge it after completion or expiry. Keep durable title/source text, not raw screenshot data in prompts, logs, or every chat message. Retry after expiry asks for the image again.
  - Deduplicate book editions using verified identifiers where possible, otherwise normalized title plus author. Films and series use kind plus title and year; never merge a book with its adaptation. Do not merge conflicting creators or years automatically. Resolve cropped names only when corroborated; confidence asserted by a model alone is insufficient.
  - Repeated/overlapping screenshots and concurrent retries must be idempotent through transactional writes and uniqueness rules. The same screenshot may be linked to another conversation without re-adding its works.
  - Undo removes the current import's contributions only. Preserve pre-existing entries and works retained by another batch or explicit user action. Individual remove/correct actions stay available; removing an interest never deletes an ontology entity, conversation, or unrelated attachment.
- Extraction and matching:
  - Introduce a dedicated `services/interestLibrary.js` plus a separate screenshot-reader service. Proposed authenticated routes under `/api/convos/:id/interest-imports` create/read/cancel/undo batches; owner-scoped library routes list, correct, and remove entries. Register through the existing route setup; keep services independent from UI rendering.
  - Read screenshots with the app's configured, available image-reading lane as a background import task. Use existing provider/credential and cost policies; do not add paid services or silently change the chat model. If image reading is unavailable, leave a retryable import failure while ordinary chat still works.
  - Extraction returns validated structured rows: kind, observed title, observed creator, observed year, visible identifier, and uncertainty. Screenshot text is untrusted data, never instructions to run tools, modify settings, or add unrelated facts.
  - Recognition of a saved-media list is required before automatic saving. Arbitrary screenshots, book titles mentioned in a conversation screenshot, and model-generated recommendations are not automatically evidence of Antoine's interests.
  - Legible title and kind can be saved without external metadata; missing author/year remains unknown. Distinguish a faithfully transcribed interest from a verified catalogue match. Metadata lookups may enrich later without blocking import or inventing details. Validate external URLs and escape all extracted strings before rendering.
  - No direct IMDb scraping, new API account, or guaranteed live catalogue access is required for this first release. Display verified links only when obtained from an actual source; generated search links must be labelled as searches.
- Access across models:
  - Add bounded `search_interest_library` and `read_interest_item` tools in `studioTools.js`, scoped to the current owner. Include origin and certainty with each result.
  - Add deterministic retrieval of a small relevant subset in the shared `buildTurnPrompt` path too, so models without tool support still receive useful interests. Preserve existing prompt budgets and provider behavior; never paste the whole library into every turn.
  - A request about “my books” or “my films” should retrieve the requested kind. Ordinary conversation should only surface clearly relevant items, not force an interest reference into every answer. Keep interest context below direct user instructions and shared memory preferences in priority.
  - The model may explain why a saved work relates to the conversation, but must distinguish title/description knowledge from having the full work. A screenshot provides neither the book text nor the film, and cannot support invented quotations or scenes.
  - Saving media is not publishing entities into the ontology or app core. Linking to an existing entity can be optional metadata; creating new entities requires the existing separate workflow.
- Library interface:
  - Add an Interests view within the existing right-column idiom, with small filters for All, Books, Films, and Series, plus search and pending review. No extra permanent navigation strip or floating button.
  - Reuse existing sidebar width/open-state persistence and `initSideResize` if a panel change requires it. Preserve drafts, active conversation, and reading settings. Keep brief status text deterministic where possible.
- Acceptance cases for implementation, following current repo verification and shipping rules:
  - Mixed screenshot imports books/films/series, preserves all visible rows, and requires no confirmation for clear entries. Example titles: Crook County, Crime Fictions, Privilege and Punishment, High Rise Stories, Ghettoside, Misdemeanorland, Crack, The Two America's, Until We Reckon, When Crack Was King; films Menace II Society (1993), City of God (2002); series Snowfall (2017–2023), Top Boy (2011–2023). Preserve displayed wording; do not infer unseen subtitles.
  - Overlapping screenshots containing Torn Apart and Mass Supervision create one work each with multiple sources. Books and screen adaptations remain separate. A title cropped beyond safe identification waits for review without blocking the batch.
  - Ordinary screenshots remain attachments; malicious screenshot text cannot direct tools. Corrupt/oversized images, unavailable image reading, invalid extraction output, cancellation, and duplicate retries never freeze a thread.
  - Imports survive refresh and model changes. Switching threads during a read cannot misattribute a batch. Unauthorized batch/library access is rejected. Undo leaves previous interests intact.
  - Both Room and Side Talks, across tool-enabled and tool-less models, can retrieve a relevant saved interest and correctly call it an interest rather than something read or watched.
  - Verify compact UI in its actual open-sidebar state and a narrow window. Copy any frontend implementation to `queue-server/public/index.html`; do not change frontend files as part of this planning task.
- No choice from Antoine blocks this plan. Build-time details still to resolve against the current app: temporary image-storage implementation and which existing right-column control hosts Interests. Keep those choices within the behavior above. Amazon list links, account connections, full-text ingestion, automatic external list syncing, and bulk ontology creation remain outside the first release.
