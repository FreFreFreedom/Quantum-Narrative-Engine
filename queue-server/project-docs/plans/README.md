# Plan Backlog

Permanent home for implementation plans for QNE. Each file here is a **self-contained
brief**: enough context, decisions, file references, implementation detail, risks and
testing steps that a coding agent (Claude Code, OpenCode, or another) can implement it
later without the conversation that produced it.

**Audited 2026-08-19** against the actual code, not against these labels. Five of twelve
statuses were materially wrong — two said PLANNED about work that was largely shipped and
running. What follows is what the code says. The percentages are rough but the named
"what's left" is specific and was verified.

**Re-audited 2026-09-13.** Every row previously marked DONE with nothing left was checked
against the live code again (grep for the concrete artefact, not the label) and cleared
out of this table — they add no signal once verified complete. Two real problems turned
up doing this: `command-center-dashboard.md` was marked DONE but the feature it describes
was quietly removed from the app two days later, and `room-analogy-engine.md`'s own
follow-up fix (unlimited count, remembers the subject) was actually built and tested live
on a queue branch that never got merged to `develop` — both corrected below.

---

## Open work

Each row names the actual remaining piece. Rows that were fully done with nothing left
have been cleared out — see "Working rules" below for how to re-check before trusting any
status here.

| Plan | What is left | State |
|---|---|---|
| [amazon-book-lists-in-the-room.md](amazon-book-lists-in-the-room.md) | Everything. Import Antoine's custom Amazon book Lists—including books never bought—into a QNE-owned Interest Library without storing Amazon credentials or pretending saved means read. Amazon has no supported consumer read API for custom retail Lists, so the first path is HTML/share-link import and the later one-click path reads the list inside his already signed-in browser. Room and Side Talk models retrieve only the relevant few books through a bounded tool, while the existing board can keep one beside a conversation. | **PLANNED — not a green light** 2026-09-17 |
| [room-conversation-links-and-merges.md](room-conversation-links-and-merges.md) | A reference attaches a fixed, refreshable snapshot of another Room thread or Side Talk as lasting context, either through Attached or by dragging a thread from the left list onto the open chat. A merge creates a new descendant from two to six sources, keeps every original untouched, opens with a bridge across their agreements, differences and live questions, and retains permanent origins. | **IMPLEMENTED** 2026-09-16 |
| [composer-redesign-and-quote-chip.md](composer-redesign-and-quote-chip.md) | Everything. The shared chat composer (`studioEmbed`, used by Side Talks, the Room's main chat, and every other embedded conversation) needs its model-picker row redesigned so it stops crushing the text input, its action-links row collapsed behind a toggle the way the Room already does, an attached quote's send path fixed so the transcript shows the existing small collapsed chip instead of the full quoted text, and one shared font size everywhere. Frontend only, one shared component fixes every panel. | **PLANNED** 2026-09-14 |
| [quota-aware-engine-picker.md](quota-aware-engine-picker.md) | Everything. When a queued task doesn't have an explicit provider/account pinned, automatically pick whichever Claude account (main or side) has the most subscription quota left, instead of a fixed default or Antoine noticing and switching by hand. A failed/429 quota read must count as "unknown," never "zero" — the side account 429s often and must not get permanently avoided because of it. Explicit choices (a plan, a manual patch) always win over this default. Codex quota tracking is a separate task and out of scope here. | **PLANNED** 2026-09-14 |
| [runner-watches-the-queue.md](runner-watches-the-queue.md) | Everything. The always-on local runner (not a chat session, so it survives closing any terminal or switching accounts) gets two new free Slack/desktop notifications on top of the ones it already sends: one when a genuinely new task lands in the queue, one when a new task looks like a likely duplicate of one already active. Both flag-only — no auto-cancel, no auto-reassign, matching Antoine's answer that the system should watch and tell him, not fix things on its own. | **PLANNED** 2026-09-14 |
| [flow-stability-and-interrupted-task-history.md](flow-stability-and-interrupted-task-history.md) | Flow no longer treats background queue, suggestion, and Seed updates as a reason to replay every card's entrance animation; it coalesces them, preserves the reader's scroll position, and keeps stopped task attempts in Done. Continuing one now creates a linked queue row, so the interrupted attempt remains visible with its outcome. | **IMPLEMENTED + deployed** 2026-09-14 |
| [room-side-talks-fix-flow.md](room-side-talks-fix-flow.md) | Aside now attaches a selected quote without sending it, focuses the new side talk, exposes its existing model picker, and brings smart titles to new side talks plus a placeholder-title backfill. | **DONE** 2026-09-14 |
| [room-side-talk-title-quality.md](room-side-talk-title-quality.md) | Side Talk titles must name the real subject, not accept vague fragments such as “The nature” or “Fields for”. Tightens the title request and validation, retries once when a title is unusable, then repairs only weak automatic titles already in the list. | **IMPLEMENTED + deployed** 2026-09-14 |
| [room-owner-emphasis-and-core-publication.md](room-owner-emphasis-and-core-publication.md) | Everything. Every Room Remember action becomes a guided capture where Antoine can state what he means, mark it Central, and save to Memory or directly to Core paradigm. His note shapes the remembered concept rather than sitting beside it; explicit Core saves become dated core additions through the Mac runner, with no second review gate, and automatic harvests remain unable to touch the core. | **DONE** 2026-09-14 — implemented; live-Room drive-through not verified (no browser in this session) |
| [room-clarifying-questions-and-interview-mode.md](room-clarifying-questions-and-interview-mode.md) | Everything. Every Room lane may ask one question when a material ambiguity blocks a good answer; Antoine can also enter a compact, persistent per-thread Interview mode by control, natural language, `/interview`, or the retained `/grill-me` alias. It stays with the thread across model changes, asks one living question at a time, and only Antoine ends it with **Answer now** or equivalent words — then the Room synthesises the interview and returns to normal conversation. | **DONE** 2026-09-14 |
| [run-queue-tasks-in-parallel.md](run-queue-tasks-in-parallel.md) | Everything. The Dispatch Queue's local runner only ever runs one task at a time today even though per-task worktrees, branches, and the ship lock already make parallel work safe — turn the runner's main loop into a bounded pool (reusing `MAX_CONCURRENT_WRITERS`), add a take-turns guard for question-mode tasks (they share one folder, unlike coding tasks), and move publishing/cleanup onto their own timer so a full pool doesn't starve shipping. | **DONE** 2026-09-14 |
| [room-analogy-engine-social-structure.md](room-analogy-engine-social-structure.md) | Everything. Reform the Room analogy pane so social structures are the default at any scale, while explicit requests can still reach scientific domains. Port the completed but unmerged natural-request work from `76c301b`, preserve the later forget/regenerate controls already on `develop`, and add an independent critic that rejects shallow or merely clever matches before a compact scale-labelled card appears. | **PLANNED** 2026-09-13 — approved and sent to the queue |
| [room-analogy-engine-natural-requests.md](room-analogy-engine-natural-requests.md) | **A working version of this already exists** — commit `76c301b` on branch `queue/natural-requests-and-lasting-context-for-92e9c950`, tested live (asked for 8, then 20 more, then stopped a 60 mid-way, all worked). It never got merged to `develop` because later commits (delete/regenerate/forget-analogy controls, the room board) touched the same files first and it now conflicts. Port that branch's logic forward rather than rebuilding it from this plan text. | **DONE** 2026-09-14 — ported and live on develop |
| [room-analogy-mockups.md](room-analogy-mockups.md) | Exploratory design only: analogies beside the conversation, paired structures, and readings across scales. Three interactive mockup states use explicitly fictional sample content. Placement, evidence depth, and how an analogy enters the discussion remain open. Not authorization to build the live recommender. | **PLANNED** 2026-09-13 |
| [codex-as-an-engine.md](codex-as-an-engine.md) | Everything. A fourth engine: Codex in the task queue as a real tool-using coding lane, and OpenAI's model as a pick in the Room — both drawing on the ChatGPT subscription through the `codex` CLI on the Mac, with no API key anywhere (his standing rule). Measured on this Mac 2026-09-13: CLI 0.154.0, default `gpt-6-astra`, `auth_mode: chatgpt`, and `codex exec` carries `--json`, `-o`, `-C`, `-m`, `-s` — everything the provider seam needs. Rides `providers/index.js` (a module beside `claudeCode.js`) and, for the Room, the existing `helper_jobs` lane, since Railway's container has no CLI. Carries the billing invariant that decides it: the spawned env must delete `OPENAI_API_KEY`, or the CLI can bill per token instead of the subscription. Also: the signed-in account is a shared work account, so the lane badge must say whose it is. | **PLANNED** 2026-09-13 |
| [every-room-conversation-in-the-repo.md](every-room-conversation-in-the-repo.md) | Everything. Every Room thread with at least three messages from Antoine is written into the repo as a readable transcript, so a terminal session or a queue agent can use past conversations as context — today only `/note`d ones reach a worktree. Rides the mirror the Mac runner already pushes every five minutes (no server-side git: Railway's image has no git binary), adds one read route, one pure `convoFiles()` list keyed by thread id, and fixes a real bug on the way: `commitFilesToTrunk`'s pruning compares basenames, so two mirrored folders each holding an `index.md` keep each other's stale files alive. No model, no cost — it copies text. | **IN PROGRESS** 2026-09-13 |
| [content-and-room-one-place.md](content-and-room-one-place.md) | Everything. Content and the Room stop being two places: one screen with the reading on the left and a shaft of ladder-rungs on the right, and neither half is steered by hand — the picture re-forms around whatever the conversation is naming, on a pause, never mid-word. Opens with the one pattern (connection against freedom) being written into `fractal_operational_core.md`, because the lens, the two-part reading and "what the conversation is circling" all read off it. Then the interior becomes the headline (Dogville re-run with stance, and the anatomy handle judged against the Dogville/Maxson gate), the tension vocabulary gets repaired (206 of 659 rows malformed, recoverable from what is already on disk), the profile is lensed by the talk with fixed headings and honest counts, and the old Graph/Landscape/List/Filters views retire into the shaft. Carries the corpus measurements taken from production 2026-09-10 — do not redo them. Two things Antoine has NOT settled: whether holding both sides is the midpoint of the line or off it, and whether each end carries one name or two. | **PLANNED** 2026-09-13 |
| [anatomy-replaces-tags.md](anatomy-replaces-tags.md) | Everything. Retires the tag system as the thing that connects entities and puts four readings in its place — locus of exile, load shift, sovereignty reversal, loop dynamics — each pointing at a part rather than claiming a label, each carrying a source and a falsifier. Written from `fractal_operational_core.md` §21 and the civic-and-justice conversation Antoine pointed at. Unlocks search by bone (acceptance test: the top match shares no word with the query), the blind-spot report the shape-audit endpoint already computes and nothing renders, the split screen, and transplants from foreign domains. Names five gaps as gaps rather than scheduling them, including the map-redraw the vision demands and nobody has a method for. | **PLANNED** 2026-09-11 |
| [testimony-in-the-ontology.md](testimony-in-the-ontology.md) | All of it, in three gated phases. Antoine's notes and harvested facts are linked to the ontology entities they name — testimony attached to entities, **never** entities themselves (`fractal_operational_core.md` §1: mediums are not entities, quoted in the plan as non-negotiable). Phase 1 is a free, deterministic, model-free matcher into a new `entity_mentions` table; phase 2 surfaces it on the entity card, a review queue and the Home card; phase 3 turns same-paragraph co-occurrence into `relation_proposals` carrying an **editable** model-written falsifier, confirmed through the existing `createRelation` so nothing bypasses `validateRelation`. Measurements are in the plan and must not be redone: 27 of 492 entities matched across the seven notes, two false positives (`She`, `The Master`), and the sentence-initial rule costs zero entities. Honest yield stated up front — 465 entities will show nothing. | **PHASES 1-2 DONE** 2026-09-09 · phase 3 still gated on Antoine seeing phase 1's result |
| [cross-domain-healing-search.md](cross-domain-healing-search.md) | All of it, and Stage 1 is a research question rather than a task — argue it with Antoine before any code. Retrieve a structural antidote from a domain with no shared words (the gut's sIgA buffering as a policing model; the burnt forest's nurse log as transitional justice). Blocked not by the missing non-narrative corpus but by an invention: **a computed anatomy handle**, since `shape` today is four codes written by hand, which is label-matching with ceremony. Candidates: frustration + camp profile, canonical form, neighbourhood refinement. Gate: a handle that gives Dogville and the Maxson household the same value has failed. **Not a green light.** | PLANNED |
| [civic-structures-and-loops.md](civic-structures-and-loops.md) | **Stages 1-3 shipped 2026-09-09**; Stages 4-6 left. Done: the ordered scale ladder (`services/scaleLadder.js`, `npm run scale:selftest`) with the graph's mis-named `vert` edge renamed `author`; the justice cluster XIII (`data-seed/civic_cluster.json` — 11 mediums, 33 characters, 34 institutions/families/cities/groups, a policy stored as a dated **posture** rather than a type); and the first anatomy on the Maxson household (`scripts/interior-fences.js`, findings in [civic-structures-first-anatomy-findings.md](civic-structures-first-anatomy-findings.md)). Left: stored relations with direction and time, saved maps + the gap audit, and intake repointed at interaction traffic. | IN PROGRESS |
| [command-center-dashboard.md](command-center-dashboard.md) | **Not live — this table previously said DONE, wrongly.** The Home view this plan describes shipped 2026-09-09, then was deliberately removed from the app on 2026-09-11 ("Home is gone: everything it summarised is the view that owns it") — the backend route is still on disk but unused, and the frontend has no Home tab, no nav entry, no renderer. If this is wanted back it needs re-deciding, not just re-flagging done. | **REMOVED FROM APP** 2026-09-11 — label was stale for two days |
| [marker-detection-calibration.md](marker-detection-calibration.md) | All of it. Investigation, no code — can a model recognise the four architecture markers (scale-jumping, three-layer talk, QNE language, integration continuum) in unlabeled prose from `fractal_vision_passages.md`, using only their definitions? Cheaper and more diagnostic than the queued `calibration-test.md`, and should run first: if a model can't spot these patterns in text written about them, subtitle-based film reading is unlikely to succeed either. Markers 1 and 9 must be reported merged (they co-occur 175/265-322 times), never as two separate scores. | **PLANNED** 2026-08-26 |
| [vertical-vs-entanglement.md](vertical-vs-entanglement.md) | Rename the graph's `vert` edge (shared director/writer) to something honest — it is not vertical navigation as the paradigm defines it (traced descent through real, causal intermediate nodes). Give the `ent` (entanglement) edge's `why` string a stated pattern-signature instead of a bare tag count. Explicitly does **not** build real vertical navigation — that's blocked on [scale-as-an-ordered-ladder.md](scale-as-an-ordered-ladder.md). | **PLANNED** 2026-08-26 |
| [scale-as-an-ordered-ladder.md](scale-as-an-ordered-ladder.md) | Give `entities.scale` (currently three unordered labels: individual/film/national) a real ordered ladder (cell→cosmos, from the paradigm) so the Scale Echo `bridge` edge can use rung distance instead of its current `type !== type` proxy — the `scale-echo` component's own stated next step. Expose `scale` as a facet in `listFacets`. Does not populate the empty rungs (cell, city, civilisation, cosmos have zero entities) and does not build vertical navigation. | **PLANNED** 2026-08-26 |
| [auto-mirror-notes-for-coding-agent.md](auto-mirror-notes-for-coding-agent.md) | All of it. The terminal coding agent (Claude Code/OpenCode) runs in a worktree off `develop` with no DB access, so it cannot read `/note` docs in `knowledge_docs`. Mirror every `Note:` doc to `queue-server/project-docs/notes/` (one file per note + `index.md`), commit+push to `develop` (rebase-and-push, debounced, only that subtree — do not fight `git-ship`), hook it from `createKnowledgeNote`, plus boot + periodic sync, and add a `CLAUDE.md` line pointing the coder at the folder. Scope: all notes, per-note files. | **PLANNED** 2026-08-25 |
| [deep-document-extraction.md](deep-document-extraction.md) | All of it. Rebuilds the shipped section-by-section PDF extraction as one thorough Deep mode: always Gemini Flash (free), high output budget, structure-aware splitting (PDF outline + heuristic, subdivided per section), fully app-aware (project map + condensed catalog of notes/seeds/suggestions/tasks/components injected every prompt, plus per-section retrieval of the most relevant docs), a richer per-section schema including loose/unconnected ideas, and a final consolidated digest written by the 2nd Claude account. Reverses the old fixed-25k-window, mechanics-only, no-app-context design. | **PLANNED** 2026-08-24 |
| [cancel-extraction-on-detach.md](cancel-extraction-on-detach.md) | All of it. Bug fix: detaching a document from the Room's attached list must stop its background extraction and clear the box. Adds `cancelExtraction()` (deletes the doc's `doc_extractions` rows) and makes `runExtractionSweep` skip rows that no longer exist, so the reader stops calling the model on a removed doc; the `DELETE /subjects/:type/:id` route cancels a detached file; the UI clears the box on detach. Reading already continues across app navigation and is already one-at-a-time (a queue). | **PLANNED** 2026-08-24 |
| [opencode-terminals-as-a-second-lane.md](opencode-terminals-as-a-second-lane.md) | All of it. The stale-worktree bug (a persistent OpenCode folder found 14 commits behind develop with nothing ever refreshing it) widened into the real ask: run a queued task by hand in an OpenCode terminal instead of the app runner, several at once in separate terminals, without conflicts, closing the loop back into the app when done. Six parts: sync the OpenCode worktree before every open plus a runner-startup backstop; make send-plan.js actually commit and push a plan instead of assuming it already happened, and do the same for app-drafted plans, since a worktree can only see what is really on the trunk; a manual-run flag so the automated runner can never grab a task a human has claimed; a new per-task command giving each manual task its own fresh worktree and branch, with a list command matching the existing runner-status feedback style; a ship command to push the finished branch into the existing branch-agnostic merge pipeline; and pointing the plan-drafting model at Sonnet, matching the never-deep ceiling already shipped everywhere else. | **PLANNED** 2026-08-23 |
| [room-turn-router.md](room-turn-router.md) | All of it. **Parts 2 and 3 of [one-chat-many-minds.md](one-chat-many-minds.md), split out and bundled together 2026-08-23** — a free-first per-turn router (`turnRouter.js`, built in `modelPolicy.js`'s image) that picks a lane per message instead of per feature, plus the three slash commands (`/ask`, `/check`, `/second`) that ride on top of it. `implement` intent is made judge-unparseable, same trick `modelPolicy.js` already uses, so only a deliberate click ever reaches the Dispatch Queue. | **PLANNED** 2026-08-23 |
| [room-side-panels-collision.md](room-side-panels-collision.md) | All of it. Found live right after Part 4 shipped: the Room's right side was only ever built for one column (Attached), but Mind (Part 1) and Ideas (Part 4) were each bolted on as their own flex column with no matching collapse toggle and no narrow-window handling — Mind's own CSS has no width at all, so its size is driven by its content and it visually collides with whatever sits next to it. Folds Mind and Ideas into the existing Attached column as stacked sub-sections instead of independent columns — restores the "two side zones" shape the file's own CSS comment already describes, with no new collapse logic or breakpoint rule needed. | **PLANNED** 2026-08-24 |
| [queue-panel-composer-typing-erased.md](queue-panel-composer-typing-erased.md) | All of it. The Queue panel's (Cmd/Ctrl+/) new-task textarea (`#qpText`) gets wiped mid-keystroke because the background poll (`qLoad()`, every 4-15s) calls `renderQueuePanel()` — which does a raw `innerHTML` replace with no saved value and no typing guard — whenever anything in the queue's data changes, which is often. Fix reuses the existing `qSnapInputs`/`qRestoreInputs` snapshot-restore helper already used for the same class of bug elsewhere, plus adds the panel to `qTypingGuard()`'s selector list. The Flow tab's separate "+ New prompt" box is unaffected — already safe. | **PLANNED** 2026-08-24 |
| [room-router-confirm-followup.md](room-router-confirm-followup.md) | All of it. Found in real use: a short confirming reply ("so can you perform this?") to a build proposal the assistant itself had just described fell through to brainstorm instead of proposing to build it, because `soundsLikeTask()` only ever looks at the current message, never the turn before it. Fixes it for free (checks the confirmation against `lastAssistantText`, already passed into `resolveTurn()` but unused) with an optional narrow judge fallback for confirmations no keyword list would catch — gated so the judge can still never independently invent `implement`. | **PLANNED** 2026-08-24 |
| [room-sidebar-fixes.md](room-sidebar-fixes.md) | All of it. Found in real use right after the Attached/Mind/Ideas merge shipped: a cluttered drop-zone crowding the "Attached" header, a collapse chevron that never updates its own icon/title so there's no visible way to tell it can reopen the panel, an empty attach-picker to diagnose live, Mind needing its own default-collapsed toggle, idea titles truncating in the narrower column, no way to resize the sidebar, and — a real product decision — world-look ideas silently replacing older rounds instead of accumulating (confirmed: `discovery_reports` already keeps every row, only the read path shows the newest). Also adds multi-select on ideas with one combined "discuss these together" message, reusing the existing `wl-mark`/`sel` selection mechanism from elsewhere in the app. | **PLANNED** 2026-08-24 |
| [one-chat-many-minds.md](one-chat-many-minds.md) | Umbrella plan, most parts split out and finished — only [room-turn-router.md](room-turn-router.md) and [room-handoff-engine-choice.md](room-handoff-engine-choice.md) (Parts 2/3 and 6) are still open above. **One Room thread, many lanes** — a per-turn router plus a shared memory, which is what "multi-agent system" actually reduces to here: models take turns in one transcript instead of conversing, so a handoff is a DB row rather than a second bill. The find that shapes the whole cost model: `repo_probe` already reads the repo with read-only git on the Mac for **zero model, zero quota**, so most "does the app already do X" questions are free. Carries the trap that memory and repo facts must sit **after** `liveListsBlock()` — ahead of the project map they would break the cache prefix and 4× every turn. | **PLANNED** 2026-08-23 — Parts 2/3 and 6 still open |
| [conversation-voice-and-project-map.md](conversation-voice-and-project-map.md) | Steps 1-2 of the parent plan below shipped in code. gpt-4.1 (4× cheaper cached input than 4o) plus a persona built on **judgement rather than register**, and a ~10k-token project map, cached, replacing the per-turn digest. **Two things left**: the AI Settings 'studio' row still has to be pointed at gpt-4.1 by hand, and the four live checks (caching visible in the spend ledger, the voice tests) need the deploy. | **DONE (code)** 2026-08-21 — two manual/deploy checks still open |
| [one-conversation-system.md](one-conversation-system.md) | Umbrella plan. Steps 2 and 6 done; Steps 1, 3, 4 split out to [roaming-conversations-backend.md](roaming-conversations-backend.md) (done); **Step 5 (the full-screen room) is the last piece**, deliberately unplanned until its endpoints actually shipped — they now have, see [the-room-to-think-in.md](the-room-to-think-in.md) above should it need revisiting. | **Steps 2 + 6 DONE** 2026-08-21 |
| [prove-the-caching-works.md](prove-the-caching-works.md) | Small and self-contained. The whole Idea Studio cost model rests on OpenAI's prompt caching giving a **4× discount** on the ~9k-token project map sent in front of every turn — and **nobody has ever verified it applies**, because `openai_spend_ledger` records `tokens_in` but not *cached* tokens. `costOf()` already prices cached tokens correctly, so billing is right; only the record is missing, which is why the claim cannot be checked. Adds one additive column, threads it through the buffered write that must never drop a write, and shows the month's cache hit rate in the concealed usage panel. Also fixes two spend-bar labels still saying gpt-4o. If the number comes back zero, every studio message has been costing 4× the design assumption. | **PLANNED** 2026-08-21 — sent to the queue |
| [make-the-caching-actually-work.md](make-the-caching-actually-work.md) | Small, and the prize is a **4× cut** in the running cost of every studio conversation. [prove-the-caching-works.md](prove-the-caching-works.md) measured the hit rate at **0%** across six live turns — a true reading, since `localMonthCacheHitPct()` returns `null` when unmeasured and returned `0`. Already ruled out: the map genuinely is first, and there is no variable prefix (one single user message, `openaiCompat.js:89`). Two live suspects: `prompt_cache_key` is never sent anywhere in `server/src`, and the per-turn cost implies a prompt nearer 3k than the ~9k the map should add — which would put it under OpenAI's 1024-token caching floor. Instrument first (prompt size, map size, raw cached_tokens), then fix. Explicitly forbids reordering the prompt: that order fixes two separate verified failures. | **PLANNED** 2026-08-21 — sent to the queue |
| [count-every-dollar-we-spend.md](count-every-dollar-we-spend.md) | **Money safety, not a feature: the $10 ceiling cannot hold because part of the spending is invisible to it.** Found while verifying the caching fix — the verification kept reading 0% because the calls it made recorded nothing; through the streaming path the same test read 33% instantly. Three defects: `runToolless` and `chatCompletion` both drop the response's `usage` block, so **every non-streaming OpenAI call is free as far as the cap is concerned** (`/plan`, `/handoff`, `/fold`, non-streamed studio answers — and once per round, so a six-round tool answer spends six times and records zero); and free-provider tokens are written into the paid ledger because `recordSpend` is called without an `isMeteredProvider` check, which also dilutes the cache hit rate. Corrects the record: **prompt caching works.** | **PLANNED** 2026-08-21 — sent to the queue |
| [let-the-ai-ask-about-the-clusters.md](let-the-ai-ask-about-the-clusters.md) | Small; the piece [theme-clusters-that-do-something.md](theme-clusters-that-do-something.md) deliberately held back because the roaming-conversations task was editing the same three files. The engine has ten lookup tools and **none of them can reach the clusters** — so the one structure that says which themes belong together, computed rather than asserted, is invisible to the thing Antoine talks to about exactly that. Two tools wired to `listTagCommunities()` and `tagCommunity()`. Carries two traps: `list_theme_clusters` must return a **summary**, since 106 clusters × 641 tags blows `toolResultCap` and comes back as JSON truncated mid-structure; and there is already a `list_clusters` meaning the 12 *film* clusters, so all three descriptions get disambiguated in the same pass. | **PLANNED** 2026-08-21 — sent to the queue |
| [sidebar-pin-lit-and-no-close.md](sidebar-pin-lit-and-no-close.md) | Small, frontend only, no cost. Two corrections Antoine asked for after reviewing `3a925d7` (the Content-sidebar pass). The ✕ he did not want is removed from the right panel, and the green dot marking a pinned panel is replaced by the pin **button** lighting up — `--c-success-tint` wash plus an `--c-accent` symbol, both already defined in each theme. Carries the two traps: the `:hover` state must be re-declared or `.uicon:hover` makes a pinned button look unpinned the moment the mouse touches it (the file warns about the same thing for `.ctrlbtn.active:hover`); and the Escape that replaces the ✕ has to go in *ahead of* the graph canvas's selection-clearing branch, or Escape loses the node instead of just folding the panel. | **PLANNED** 2026-08-22 |
| [done-cards-read-and-compact.md](done-cards-read-and-compact.md) | Small, frontend only, no cost. Two complaints about the **✓ Done** list, both verified in the code. "Not read yet" is a bold title plus a 7px violet dot placed *after* an ellipsised title, so it lands at a different x on every row and there is no column to scan — replaced by the whole card moving in both directions at once: unread goes green-tinted with a green border and a dot in a fixed left gutter, read greys out. And a closed done card is four stacked rows (~120px) — head, a three-line result box, a stage strip whose only word is "Done", and a Details fold — none of which is a preview, since clicking appends a separate panel instead. Becomes two lines (~44px). Carries the one trap: `flowQueueRow` is the single template for all five task lists and `.uc` is shared by ~10 other renderers, so every rule must be scoped to a new `.uc--fin`; and `taskPillHtml` currently suppresses the "Done" pill as a duplicate of the strip label, so hiding the strip means lifting that suppression or the card loses its status word. | **PLANNED** 2026-08-22 |
| [rotate-leaked-credentials.md](rotate-leaked-credentials.md) | Housekeeping, not a feature. On 2026-08-21 the full Railway variable set was pasted into a chat **with values**, so every token and password the deployment holds is exposed in a transcript. The **variable cleanup is DONE** (14 unused variables deleted, six of them secrets — including `RAILWAY_TOKEN` and `DATABASE_URL`, which no code ever read). What is left is **revoking them at source**: deleting a variable stops the app carrying it, but does not invalidate the credential. | **Cleanup DONE · revocation deferred on purpose** (Antoine, 2026-08-21) |
| [design-system-pass.md](design-system-pass.md) | Its **headline deliverable was never built**: there is no shared `.btn` component anywhere in `fmcns_navigator.html`, so the 10+ one-off button classes it exists to consolidate are all still one-offs — **now planned separately, see [cards-one-system.md](cards-one-system.md)**. Also outstanding: real per-view responsive behaviour (only 3 breakpoints exist, none of them collapsing Content or Map) and keyboard access on the graphs (`tabindex` appears once, on the Architecture SVG only). Spacing/type tokens and the accessibility labels **are** done. | **Phase 1 (tokens) DONE 2026-09-09** by [ui-redesign-instrument-chrome.md](ui-redesign-instrument-chrome.md) Phase 0. **Phase 2 (buttons) DONE 2026-08-21** via [cards-one-system.md](cards-one-system.md). Left: per-view responsive behaviour and keyboard access on the three graph views, which Antoine declined 2026-08-21. |
| [multi-agent-development-team.md](multi-agent-development-team.md) | Most of it shipped — parallel developer worktrees, per-agent slots, the review/publish gate, role briefs, AI settings. What is left is the **specialist roster**: 3 of the 7 named agents exist (`dev1`/`dev2`/`dev3`), and `.agents/roles/` is missing `explorer.md`, `tester.md`, `integrator.md`. Its **collaboration-strategies half is CANCELLED** — see below. | **~70% DONE · roster outstanding** |
| [self-aware-platform.md](self-aware-platform.md) | All six parts are built and live. Two loose ends behind them: (1) **the learning loop does not close** — retrospectives are written to `intel_task_lessons` but nothing ever reads them back, so no future thought or task is told "you tried this before and here is what happened"; (2) **the nightly ranked drain has no runner** — `GET /intel/drain` exists and nothing calls it, so the list is never worked. Smaller gaps: the "Acknowledge" endpoint has no button in the UI, and Part 5's content-graph panel was never built (its two buttons live in the Mind toolbar instead). | **Parts 1–6 DONE · 2 loose ends** |
| [graph-that-feels-alive.md](graph-that-feels-alive.md) | **v2, sent to the queue as 8 fragments 2026-08-22.** The Content graph is hand-drawn SVG that **deletes and rebuilds every dot, line and label as new DOM elements on all 170 animation frames** of every layout — the cause of the laggy hover (a 200ms debounce added to work around clicks being silently dropped mid-rebuild), the overlapping nodes and labels (`runSim()` has no collision force), and a layout that freezes after two seconds so nothing can be dragged. Stage 1 **engine**: canvas renderer with one dirty-flagged rAF loop, `d3-force` (vendored, see below), quadtree hit-testing, node drag+pin, real fit-to-view. Stage 2 **look**: convex-hull cluster regions, semantic-zoom bands, label collision + halo, ego view, TMDb posters past 2.5×. **Palette DONE 2026-08-22** — the twelve cluster colours were four themes pretending to be twelve strangers; now four hue families of three, authored as two hand-tuned sets because a lightness ramp that reads as considered on parchment washes out to near-white on navy. Frontend only, zero cost. Two traps: canvas gets no CSS so every themed colour must be re-read when `.dark` flips, and `forceLink` mutates the edge objects three other functions read as raw ids. | Palette done; the engine/look stages still open |
| [a-map-of-what-belongs-together.md](a-map-of-what-belongs-together.md) | All of it, and deliberately last. The embedding map — the landscape view where *nearness itself is the meaning*. Corrects the obvious approach: there are only **two** continuum axes (scored on 93 and 39 entities), so plotting them is a scatter chart, not a landscape. The real material is the **106 theme clusters already computed**, NOT the raw tags: measured 2026-08-22, **582 of the 641 tags appear on exactly one entity** and only 214 of 413 entities carry any tag at all, so cosine similarity over raw tags is almost everywhere zero and IDF makes it worse by weighting precisely the tags that connect nothing. Build the vector over **theme communities** instead — they are the collapse of that sparse vocabulary, none of them singleton, the largest holding 64 tags — then weight by community rarity and lay out with **the same `d3-force` already vendored**. A similarity map with **no new dependency, no model call, no cost**. Becomes a fourth Content view: no edges, just position, with theme clusters as the landscape's countries and the two axes demoted to an optional colour overlay. | **IN PROGRESS** 2026-08-22 — fragment 14 (the layout and the plumbing) **DONE**: Landscape is the third Content view, laid out by cosine similarity over the theme communities **plus the 59 tags more than one entity carries** — the brief's community-only vector had to be corrected, because 118 of the 214 tagged entities live entirely inside one community and would all have had the same vector. Measured: same-community pairs sit 74 units apart, unrelated pairs 388 — 5.2x. The 199 untagged entities are parked on an outer ring by roman-numeral cluster and drawn hollow, never mixed into the field. Fragment 15 (the look — community regions, labels, posters, the continuum overlay) is still to run. |
| [github-code-discovery.md](github-code-discovery.md) | MVP complete, and ~70% of the "not started" Phase 2 is in fact built (history view, save-as-Seed, entry points, queue wiring). Genuinely missing: **`architecture_node_evidence` is written but has no read path** (`GET /evidence/:nodeId` does not exist), so the evidence behind a planted idea cannot be seen; plus tech-tree evidence icons, report delete, and `/reports/:id/rerun`. | **DONE (MVP + most of Phase 2)** |
| [world-ideas-close-the-loop.md](world-ideas-close-the-loop.md) | Amends `0c26e70` (the IDEA-PROOF feature): checking works, but trouble hides until you open the right tab and an idea whose one model check missed stays `not_checked` forever. Four items: an amber ✨ badge on finished cards whose picked ideas are unusable (per-card count added to `GET /prompts`); after each ship the Mac runner settles up to two oldest leftover unchecked tasks through the existing `/ideas-landed/unsettled` + `/verdicts` endpoints (bounded, never on the request path); "Already queued to be finished" rows become clickable; and a webhook nudge only when the unfinished count rises 0→N. Carries the standing rules: an idea gap never blocks a ship, `not_checked` never reads as failure. Antoine declined any change to the pick/Apply screen. | **PLANNED** 2026-08-23 |
| [theme-follows-the-mac.md](theme-follows-the-mac.md) | **SUPERSEDED 2026-09-09** by [ui-redesign-instrument-chrome.md](ui-redesign-instrument-chrome.md) Phase 0, which shipped what this asked for and more: the rail's button now opens a menu of named looks with **Match my Mac** as a third choice that follows `prefers-color-scheme` live, stored as `fmcns-look` (the old `fmcns-theme` value is migrated on first load). Do not implement this as written — the light/dark cycle it describes no longer exists. | **SUPERSEDED** 2026-08-23 |

### Cancelled

| Plan | Why |
|---|---|
| [multi-agent-development-team.md](multi-agent-development-team.md) — *collaboration strategies only* | Antoine's decision, 2026-08-19: "not relevant anymore". The Single / Competition / Team picker was removed from the New-prompt form, and the `task_stages` table that would have driven it (zero reads, zero writes since it was created) is no longer created. Picking Competition or Team had been storing a value, setting a state nothing read, and running one single agent while quoting a 2.5×–4× cost. The rest of this plan stays open — see above. |

---

## Shipped

Plans confirmed fully implemented, nothing left. Kept for their reasoning and traps, not
tracked as open work.

| Plan | What it is | Note |
|---|---|---|
| [attachments-in-room-composers.md](attachments-in-room-composers.md) | One paperclip in the main Room and every side talk: images ride with the message to the existing vision lane; PDFs and common text files become lasting, tool-readable conversation context. | **DONE** 2026-09-15 |
| [cheap-paid-opencode-models.md](cheap-paid-opencode-models.md) | DeepSeek/Qwen/Zhipu-paid/Kimi wired into OpenCode's own `provider` config with real per-token cost, kept out of every automatic/free fallback chain; the New-Prompt/AI-Settings model pickers extended to allow just these four provider families. | **DONE** 2026-09-13 — verified live against the actual OpenCode config; Antoine still needs to add his own API keys before any of these run for real |
| [free-model-file-tools.md](free-model-file-tools.md) | The free chat-only providers can run implement-mode Dispatch Queue tasks with real file tools (read/write/edit/list, no shell), behind two required gates: an env master switch (`ALLOW_AI_ROUTER_TOOLS`, off by default) and a per-task checkbox that only appears once the switch is on. | **DONE** 2026-09-13 — sandboxing and both gates verified; two minor follow-ups noted in the code (a tight rate-limit edge case, a silent-no-op path if worktree creation ever fails) |
| [opencode-go-usage-link.md](opencode-go-usage-link.md) | Two plain links to `opencode.ai/go` — one in AI Settings' quota pane, one as a rail icon button. | **DONE** 2026-09-13 — confirmed live in production |
| [queue-world-ideas-manual-only.md](queue-world-ideas-manual-only.md) | World Ideas no longer generate automatically on task creation or the six-hour sweep — only an explicit "Look at the world" click triggers them. | **DONE** 2026-09-13 — verified in code and by its own regression test |
| [room-mood-board.md](room-mood-board.md) | A Room thread gets a board: a wall of film stills/museum images fed for free by entities the Room already scanned, kept with one tap, laid out in draggable columns. | **DONE** 2026-09-13 — table previously said PLANNED; confirmed fully built and working |
| [room-board-rhyme-with-eyes.md](room-board-rhyme-with-eyes.md) | The board's automatic line is now written by a model that can actually see the image (Gemini, vision), not one that only sees a title. | **DONE** 2026-09-13 — table previously said PLANNED; confirmed fully built and working |
| [room-side-talks-and-remember.md](room-side-talks-and-remember.md) | A second conversation beside a Room thread for tangents, plus "Remember" — select a passage and propose it as a durable fact. | **DONE** 2026-09-13 |
| [room-analogy-engine.md](room-analogy-engine.md) | Analogies arrive beside the Room conversation in their own small side pane, with a gutter dot, cards, and "↑ bring". | **DONE** 2026-09-13 — its own scope is complete; the follow-up (unlimited count, lasting subject) is tracked separately above, already built but unmerged |
| [queue-cost-cap-repair.md](queue-cost-cap-repair.md) | Subscription-model usage cannot be mistaken for metered cost after a task falls back between engines; stopped work can be retried directly on a chosen Claude account. | **DONE** 2026-09-14 |
| [codex-quota-in-the-readout.md](codex-quota-in-the-readout.md) | Codex's remaining allowance now shows in the rail beside the two Claude accounts, read from the file Codex already writes to disk. | **DONE** 2026-09-13 — table previously said PLANNED; confirmed shipped and merged |
| [queue-panel-and-element-picker.md](queue-panel-and-element-picker.md) | The build panel stops covering the page; the element picker stays armed across picks and resolves clicks to the nearest named control. | **DONE** 2026-09-11 |
| [self-diagnosis-fixes.md](self-diagnosis-fixes.md) | The four decisions of the paradigm's self-diagnosis: paired tag tensions, the always-true `grounded` flag retired, the immune system on the cell rung, QNE at the institution rung. | **DONE** 2026-09-10 |
| [theme-clusters-are-too-small.md](theme-clusters-are-too-small.md) | Real multi-level Louvain clustering — 651 tags fall into 15 theme clusters instead of 104. | **DONE** 2026-09-10 |
| [the-horizontal-move.md](the-horizontal-move.md) | The third navigation move, horizontal peers, actually findable via `services/peers.js` and a Room tool. | **DONE** 2026-09-10 |
| [where-the-corpus-does-not-connect.md](where-the-corpus-does-not-connect.md) | The gap measure between theme clusters, ranked, no threshold. | **DONE** — confirmed live (`GET /api/ontology/tag-gaps` exists and works); the table's own "what's left" text had a copy error calling it unstarted |
| [gemini-in-ai-settings.md](gemini-in-ai-settings.md) | Google's free-provider optgroup and doc-extraction row in AI Settings. | **DONE** 2026-09-09 |
| [narrative-mirror.md](narrative-mirror.md) | The Room can see the civic corpus; both anatomy runs write out the verified turns; a relation row opens onto its scene in the Room. | **DONE** 2026-09-09 |
| [ui-redesign-instrument-chrome.md](ui-redesign-instrument-chrome.md) | The whole-UI redesign: two switchable looks, self-hosted type, a quantised size/radius/stacking system, the realtime WebSocket actually wired in, the Room as a reading surface, shared card shells everywhere. All six phases. | **DONE** 2026-09-09/10 |
| [entity-interior-first-anatomy.md](entity-interior-first-anatomy.md) | Gave Dogville's town an inside via interaction-network extraction from a real subtitle file. | **DONE** 2026-09-07 |
| [google-models-and-one-shared-memory.md](google-models-and-one-shared-memory.md) | Gemini Pro as a pinned-only Room pick; `AGENT_MEMORY.md` and `mind_facts` mirrored into one shared memory. | **DONE** 2026-09-07 |
| [room-chat-tool-parity.md](room-chat-tool-parity.md) | The Room's non-streaming chat path gets the same tool access as the streaming one. | **DONE** — confirmed merged to `develop`; table previously said PLANNED |
| [fix-google-model-abort-in-room.md](fix-google-model-abort-in-room.md) | Fixed Room chat turns pinned to Google aborting after 150s. | **DONE** 2026-08-26 |
| [start-fresh-shows-no-sign.md](start-fresh-shows-no-sign.md) | "Start fresh" now visibly confirms the fold happened. | **DONE** 2026-08-26 |
| [start-fresh-keep-conversation-visible.md](start-fresh-keep-conversation-visible.md) | "Start fresh" compacts context instead of deleting the visible transcript. | **DONE** 2026-08-25 |
| [chat-model-picker.md](chat-model-picker.md) | A manual per-conversation model picker in the Room composer. | **DONE** 2026-08-24 |
| [room-shared-memory.md](room-shared-memory.md) | `mind_facts`, `services/mind.js`, the `recall` tool, and a Mind panel — a fact stated once is known in every future thread. | **DONE** 2026-08-23 |
| [room-world-ideas.md](room-world-ideas.md) | Background world-look ideas keyed to a Room conversation, with a "use this idea" button. | **DONE** 2026-08-24 |
| [local-preview-and-deploy.md](local-preview-and-deploy.md) | A finished task can preview locally before deploying, with a Deploy/Discard bar. | **DONE** 2026-08-24 |
| [queue-task-second-account.md](queue-task-second-account.md) | A real coding task can run under the second Claude account. | **DONE** 2026-08-24 |
| [ai-settings-second-account-default.md](ai-settings-second-account-default.md) | "Claude — second account" as a real default choice in AI Settings. | **DONE** 2026-08-24 |
| [files-in-the-room.md](files-in-the-room.md) | File upload into the Room actually works (a first pass shipped broken, then was fixed). | **DONE** 2026-08-23 |
| [one-card-for-a-plan-in-parts.md](one-card-for-a-plan-in-parts.md) | An umbrella card for a plan split into parts (shipped inert once, then fixed). | **DONE** 2026-08-23 |
| [plans-in-the-room.md](plans-in-the-room.md) | Plans are a seventh attachable subject type in the Room. | **DONE** 2026-08-23 |
| [roaming-conversations-backend.md](roaming-conversations-backend.md) | Multi-subject conversations, the unified tool loop on both chat lanes, `/seed` and `/note`. | **DONE** 2026-08-21 |
| [theme-clusters-that-do-something.md](theme-clusters-that-do-something.md) | Theme clusters recomputed live from the DB and actually surfaced on the entity card. | **DONE** 2026-08-21 |
| [the-room-to-think-in.md](the-room-to-think-in.md) | A third Core sub-view, the Room, reusing existing card/embed renderers. | **DONE** 2026-08-21 |
| [the-app-should-know-what-is-live.md](the-app-should-know-what-is-live.md) | The app re-checks git directly instead of trusting its own second-hand ship state. | **DONE** 2026-08-22 |
| [cards-one-system.md](cards-one-system.md) | One card shell, four button styles, one status pill, one obvious action per card. | **DONE** 2026-08-21 |
| [cards-rest-and-narrow-screens.md](cards-rest-and-narrow-screens.md) | The rest of the card conversion, plus narrow-window slide-overs. | **DONE** 2026-08-21 |
| [suggestions-that-keep-up-with-the-code.md](suggestions-that-keep-up-with-the-code.md) | Suggestions refresh on change instead of on a clock. | **DONE** |
| [an-architecture-that-knows-what-it-is.md](an-architecture-that-knows-what-it-is.md) | A witness-and-lifecycle system for architecture nodes, plus the packed map and lifecycle board. | **DONE** 2026-08-22 |
| [four-small-motions.md](four-small-motions.md) | Three small entrance motions picked off a motion-reference shelf built live in a terminal session: the Content card lifts in rather than popping, the shared dropdown menu gets an actual open animation, and a Room answer that didn't arrive over the streaming path reveals paragraph by paragraph instead of appearing whole. Frontend-only. | **DONE (3/4)** 2026-09-11. A fourth (cross-fading between Content/Map/Architecture) shipped, was found broken, was fixed, then Antoine reverted it on taste alone same day. |
| [fractal-vision-paradigm.md](fractal-vision-paradigm.md) | Filed the marker-indexed extraction of `Fractal_Vision_Paradigm_Extraction.pdf` as two standing reference docs. | **DONE** 2026-08-26 |
| [script-coverage-test.md](script-coverage-test.md) | Investigation only — checked whether film scripts/subtitles are actually gettable for the real 199-film corpus. | **DONE** 2026-08-26 |
| [obsidian-prior-art-findings.md](obsidian-prior-art-findings.md) | Scanned Obsidian plugins for prior art on gap-finding and multi-move navigation. | **DONE** 2026-09-09 |
| [tasks-start-without-waiting.md](tasks-start-without-waiting.md) | A task no longer waits through a preamble before it runs. | **DONE** 2026-08-21 |
| [send-a-plan-from-the-terminal.md](send-a-plan-from-the-terminal.md) | `npm run plan:send` — a plan deliberated in a terminal goes into the Dispatch Queue as a real task. | **DONE** 2026-08-21 |
| [brainstorm-world-ideas.md](brainstorm-world-ideas.md) | Turns a proposed world idea into a conversation with `/fold`, `/more`, `/reframe`. | **DONE** 2026-08-20 |
| [ranked-next-steps.md](ranked-next-steps.md) | One ranked answer to "what do I build next?", replacing about fifteen competing ones. | **DONE** 2026-08-19 |
| [always-on-models.md](always-on-models.md) | Free-provider gateway + quota-exhaustion ledger so every feature always has a model. | **DONE** |
| [dispatch-queue-free-model-fallback.md](dispatch-queue-free-model-fallback.md) | Queue coding jobs walk Claude's tiers then every free OpenCode model instead of pausing. | **DONE** |
| [plan-first-queue-and-idea-composition.md](plan-first-queue-and-idea-composition.md) | Part A: every queue task is auto-drafted into a plan before it runs. | **Part A DONE**; Part B superseded by later overnight-agent work |
| [travaux-quick-panel.md](travaux-quick-panel.md) | Right-anchored slide-over Queue panel reachable from anywhere. | **DONE** |
| [idea-studio-conversational-task-creation.md](idea-studio-conversational-task-creation.md) | Idea Studio — a conversational widget that grills an idea and hands it off as a paused task. | **DONE** |
| [universal-conversations-core-architecture.md](universal-conversations-core-architecture.md) | Conversation as a universal primitive across components, tree nodes, Seeds, Suggestions. | **Backend DONE**; frontend superseded by the Idea Studio modal |
| [core-workspace-unified-flow.md](core-workspace-unified-flow.md) | Merged the CORE ARCHITECTURE sub-tabs into one workspace, night mode for the whole app. | **DONE**, shipped as two zones rather than three |
| [room-handoff-engine-choice.md](room-handoff-engine-choice.md) | `/handoff claude` or `/handoff opencode` lets you name the engine a handoff goes to. | **DONE** — table said PLANNED, confirmed actually built 2026-09-13 |
| [pdf-section-extraction.md](pdf-section-extraction.md) | Backend for reading a huge PDF section-by-section, cheaply. | **DONE** — both halves confirmed live 2026-09-13 |
| [pdf-extraction-frontend.md](pdf-extraction-frontend.md) | The Room panel for section-by-section PDF extraction: progress line, confirm/reject per chunk, collapsible. | **DONE** — table said PLANNED, confirmed actually built 2026-09-13 |
| [use-saved-note-as-conversation-context.md](use-saved-note-as-conversation-context.md) | A saved `/note` can be attached to a Room thread as ongoing context. | **DONE** — table said PLANNED, confirmed actually built 2026-09-13 |

---

## Statuses

- **PLANNED** — written and approved, not started.
- **IN PROGRESS** — implementation underway.
- **DONE** — implemented and verified.
- **SUPERSEDED** — the need was met, but by a different design than this doc describes.
  The doc is kept for its reasoning; **do not implement it as written**.
- **CANCELLED** — decided against; kept for the reasoning.

## Working rules

- **Nothing here is implemented until Antoine explicitly asks.** A plan landing in this
  folder is not a green light.
- **When a feature is finished being planned**, save the final approved plan here as a
  new Markdown file with a descriptive kebab-case filename, and add a row to the right
  table above.
- **When Antoine says "implement \<plan name\>"**: read the saved plan first, then inspect
  the current state of the project and verify the plan still fits — file paths, function
  names, line references and assumptions all drift. Report anything important that has
  changed *before* writing code, then implement.
- **Update the status when work FINISHES, not only when a plan is added.** This is the
  rule that was being broken. This file had been edited 15 times and every single edit
  came from the session that *added* a plan; none came from a session that completed one.
  So finished work silently kept its old label, and by 2026-08-19 two plans claimed
  "PLANNED" about features that had been live for days. If you ship something a plan
  covers, move its row and say what is left — in the same commit as the code.
- **A plan is not evidence.** Before trusting any status here, check the code: grep for
  the concrete artefacts the plan promises (table names, endpoints, service filenames,
  function names). That is how this audit was done and it is the only thing that works.
- **A fully DONE row belongs in Shipped, not Open work.** Once verified with nothing left,
  move the row down rather than leaving it cluttering the open list — that's what the
  2026-09-13 re-audit did, clearing ~40 confirmed-complete rows out of Open work. Two
  traps found doing this: a status can be stale in *either* direction (something shipped
  and never relabeled DONE, or something labeled DONE that was later quietly removed from
  the app) — check the live code each time, don't just trust which table a row sits in.
- **Prefer "SUPERSEDED" to "DONE" when the design changed.** Several plans here were
  satisfied by a different implementation than they specify; marking those DONE invites
  the next agent to "finish" them by rebuilding what already exists differently.
- Each plan carries its own status in a header table at the top of its file. That header
  and this table must agree — three of them disagreed before the first audit, and the
  2026-09-13 re-audit corrected two more (`command-center-dashboard.md`,
  `room-analogy-engine.md`'s cross-reference).
- **This folder is for plans only.** Large external reference documents do not belong
  here; three of them once made up two-thirds of it. Keep such material next to what it
  documents (as `queue-server/SPEC.md` is), or drop it once it has been used.
