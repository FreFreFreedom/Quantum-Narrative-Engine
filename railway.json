{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "queue-server/Dockerfile"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
