- Status: PLANNED — not a green light to build or send to the queue. 2026-09-17. This is a standalone implementation brief. Antoine has settled timing, conversation scope, and saving below; no user choice blocks the plan.
- Goal: recommend books, films, and TV series using Room conversations, Side Talks, and the imported interest library. Recommendations may fit Antoine's taste, his present questions, a structural analogy in the QNE paradigm, or several of these together. None is required to challenge him, advance his thinking, or be unfamiliar in style.
- Antoine's corrections are requirements:
  - Do not make “move your thinking forward rather than reflect your taste” the governing goal. A work that feels right for him is valuable in itself.
  - Structural analogies are wanted: shared relationships, pressures, and patterns across different settings and scales, not simply shared subject words.
  - Explain the meaningful connection; do not append “where the comparison stops,” a mismatch section, or a compulsory caveat to every recommendation. Internal evidence checking remains necessary; the visible recommendation is not a comparison audit.
  - Saved titles mean interested, not bought, read, watched, liked, or endorsed. Assistant suggestions in a transcript are not Antoine's preferences unless he adopts them.
- Shared-library revision, 2026-09-17: `plans/room-reference-library.md` now owns Save, Attach, Library browsing, typed references, passage integration and migration across media, Papers and Apps. Its shared interaction/storage rules override media-only assumptions below. This file continues to own media recommendation selection; the Papers/Apps selection rules and title-only paper display stay separate.
- Antoine's confirmed choices in this session:
  - Arrival timing: automatically in a quiet side panel. No manual request required and no unsolicited recommendation messages in the chat.
  - Conversation scope: the open Room conversation and its Side Talks, plus his imported interests. Do not mine every other Room conversation. If opened from a Side Talk, resolve its parent Room thread as the recommendation scope; handle nested side relationships without crossing into unrelated threads.
  - Every recommendation has a visible Save button. His words: “add the recommendations i like to the list of those I imported ... some kind of library i can later browse.” Saving and screenshot importing feed the SAME lasting library, not separate collections. Saved recommendations join future interest signals.
  - Also provide Attach on library items and recommendations: Antoine wants to attach selected works to the prompt he will send next. This is a next-message action, distinct from saving a lasting interest.
  - No further product choice is required before a later implementation request. A pause control is an implementation safeguard for automatic work, not a replacement for his selected automatic behavior.
- What exists, with line numbers inspected 2026-09-17; search named functions because lines will move:
  - `queue-server/server/src/services/interestLibrary.js:224`: `listInterests`; around 234, `readInterest`; around 257, `interestContext`. Owner-scoped books/films/series, screenshot provenance, editable interested/read/watched/bought states, duplicate handling, and review already exist. Current conversation retrieval matches title/creator words; it does not implement this richer recommender.
  - `queue-server/server/src/services/conversations.js:915`: `listSideTalks`; around 944, `createSideTalk`; conversations use `parent_convo_id`. Use actual parent relationships, not matching thread titles. `buildTurnPrompt`, `studioTools`, and `studioDispatch` are the shared model integration points.
  - `queue-server/server/src/services/roomAnalogies.js:125`: `getContext`; `normalizeContext` describes a standing subject, central relation, focus, positions, relations, and open question. Inspect and reuse applicable structure, but do not call the existing analogy generator as a shortcut: its output and user controls belong to another feature.
  - `queue-server/server/src/services/books.js:19`: entity-based book prompt; around 39, Google Books metadata lookup; around 59, cached recommendations. This recommends from an entity, not conversations or personal interests. Its first catalogue result is enrichment, not sufficient proof of identity or structural relevance. Preserve existing entity-book behavior.
  - `fmcns_navigator.html:9520`: `ROOM_PANES`; around 12249, `renderInterestLibrary`; `roomInterests` sits in the existing resizable right column. Extend this surface instead of adding another permanent toolbar, bubble, or sidebar.
  - `queue-server/server/src/routes/conversations.js`: existing authenticated interest-library routes. Authenticated owner comes from `req.user.sub` (with existing compatibility handling), never the request body.
  - `queue-server/server/src/services/ai/style.js` and `ai/voice.js`: shared user-facing style and paradigm voice. Attach both to meaning-writing prompts; give the short card's length rule priority. Antoine's current bullet-only/plain-word preferences and direct memories override older general voice guidance, including avoidance of immune-system metaphors unless asked.
  - `queue-server/data-seed/docs/fractal_operational_core.md`, section 1: books and films are media carrying stories; the people, groups, and institutions within them are the entities whose structure can be compared. Do not turn every recommended title into a new ontology entity.
- How selection should work:
  - Build a bounded, owner-scoped reading of the selected conversations. Capture Antoine's questions and stated interests, plus taste signals such as atmosphere, pacing, setting, character relationships, and narrative style. Preserve uncertainty: discussing a topic does not mean enjoying every depiction of it.
  - Read Side Talks as their own strands, not disposable summaries under the main thread. Keep source message IDs and thread IDs with every signal. Respect corrections, rewinds, deleted messages, deleted conversations, and changed scope; recompute or invalidate affected signals instead of preserving rejected preferences forever.
  - Imported titles add explicit interest signals. Read their stored state and provenance. Screenshots supply names and visible metadata, not full book text, scenes, plots, or proof of appreciation. Titles awaiting import review are not resolved works.
  - Maintain separate candidate reasons for taste, conversational relevance, and structural analogy. Allow any of these to justify a recommendation; do not force fixed quotas or make novelty/challenge a hidden ranking objective. Optional steering: closer to my taste, more structural connections, or a user-written direction.
  - Structural retrieval represents roles and their relationships, what pressure sustains the arrangement, and what changes under that pressure. A courtroom and a family can connect through loyalty protecting the group while making truth dangerous. This is an illustrative selection pattern, not a verified claim about a particular work.
  - Compare relationships at person, family, institution, or wider scales. Shared vocabulary such as justice, power, or identity alone is not enough to label a match structural. A genuine taste-based pick remains valid without manufacturing an analogy for it.
  - Distinguish finding candidates from substantiating them. Use reliable work descriptions, existing sourced corpus readings, or available text excerpts for the proposed relation. Catalogue metadata can establish that a title exists; it cannot establish the structural interpretation by itself. Reject unsupported specific claims internally without displaying a routine “limits of this analogy” paragraph.
  - Discovery must reach beyond the imported library. Exclude already saved titles from new recommendations using work identity, including title/creator/year and verified identifiers where available. A separate optional “From your library” view can resurface an existing interest without claiming it is new or assuming it has been read.
  - Deduplicate across formats without confusing distinct adaptations. Keep a book and its film separate; collapse duplicate editions of the same book where identity is established. Distinguish remakes and series with the same title. Persist dismissal and recent exposure so the same suggestions do not recur on every request.
- Delivery and interface:
  - Placement: follow `room-reference-library.md`: rename Interests to Library and include saved media, papers, app ideas and passages. Keep current Papers and Apps destinations unchanged. Put new media discovery within this existing surface using its own mode, not a second saved-media library or duplicated tab strip.
  - Start with a small mixed automatic batch, proposed three to five worthwhile items, not one mandatory item from each medium. Return fewer if fewer are supported. Automatic arrival is the first release; no request-more workflow is required.
  - Each item: title, author or year as appropriate, media kind, verified work link when available, and one short bullet explaining its relevance. A structural recommendation says what pattern connects; a taste pick says what fits. No mandatory labels, numerical scores, review essay, or comparison-limit section.
  - Keep source-conversation links behind a small provenance action, rather than repeating thread titles on every card. Do not expose private conversation text to catalogue providers; send only the minimum title/author or discovery query needed.
  - Save is a visible one-click button on EVERY recommendation, not hidden under a menu. It adds the work to the existing interest library with recommendation provenance, without claiming a screenshot source or marking it watched/read. Show Saved only after the server confirms persistence; retries and double-clicks must not create duplicates. An already saved work shows Saved rather than offering another copy.
  - Keep the saved title, author/creator, kind, year/identifiers when verified, link, short recommendation reason, and source-conversation reference. Save creates explicit interest evidence, not proof he endorses every theme or character. Future recommendations can use these chosen works alongside imported ones. Simply displaying a recommendation never adds it to the library or trains a taste preference.
  - The combined library survives refreshes, model changes, and conversation switches. Removing an item from the library uses its existing removal controls and never deletes its source conversation. Saving remains valid if the recommendation batch is replaced or its source conversation is later deleted; remove inaccessible source links and conversation-derived snippets while retaining the explicitly saved work.
  - Attach works on both library items and unsaved recommendations. Add a compact removable chip beside the draft in the intended Room or Side Talk composer, without sending, rewriting the draft, or saving the work automatically. Support multiple selected works and suppress duplicate chips.
  - Target the composer Antoine is actively using. Preserve that target while he clicks into the library; use the visible main Room composer when there is no active Side Talk. If the intended target becomes unavailable, require a new target instead of silently attaching to another conversation. Identify the destination briefly if both composers are visible.
  - Pending media chips persist with that conversation's browser draft through refreshes and switches. Removing a chip changes only the draft, not the library. Keep chips and text after a failed send; clear the submitted chips only when the message is accepted. Newly attached chips while another send is underway must not be cleared by that older send.
  - At send time resolve owner-scoped item/recommendation IDs on the server and store a bounded text snapshot with the user message: title, kind, author/year, known interest state, verified link, short reason when present, and provenance. Do not trust client-supplied metadata or claim full-book/film access. Every provider receives these selected references as message context, including models without tool support. Render the same compact references in the saved transcript.
  - Missing or removed source items must be identified before sending rather than silently omitted. Historical snapshots already sent remain intelligible when a library item is later changed or removed; do not retain deleted private conversation excerpts in those snapshots. Bound the number/size of attachments explicitly and tell Antoine when the limit is reached instead of truncating silently.
  - Dismiss hides the recommendation; “Already read/watched” updates explicit state without assuming liked. Optional short feedback lets Antoine distinguish not interested, wrong connection, or simply not now.
  - Do not treat a dismissal as dislike of an entire topic. Avoid inference loops where the model's own recommendations become new preference evidence just because they appeared in a conversation.
  - Keep reasons spoiler-light by default. Do not reveal endings, twists, or late-series developments to explain a structural match. Offer further detail on request, within any explicit spoiler preference.
  - Automatic generation works after meaningful conversation changes and a quiet pause, not while Antoine is typing or on every repaint. Refresh the active Room scope only; closing the panel need not discard queued work, but inactive unrelated conversations do not trigger fresh discovery. No unsolicited message inside the chat. Preserve a batch currently being read; indicate newer suggestions quietly instead of reshuffling under his pointer. Store batches per Room scope so switching threads cannot show another thread's picks as current.
- Implementation pieces:
  - Add a media-specific generator/adapter, potentially `queue-server/server/src/services/mediaRecommendations.js`, behind the current `roomRecommendations.js` orchestration. Reuse its existing history/request/scheduling machinery where compatible; do not build another competing worker. Preserve the live paper/app generator's policy and strict provider setting. Add a separate structural/taste context facet where the research summary would lose relevant detail.
  - Extend existing recommendation tables only for needed media settings, signals, evidence and feedback. Shared saves belong to `referenceLibrary.js` as specified in `room-reference-library.md`, not fake screenshot-import records or duplicate saved-item tables. Use transactional saves, request identity keys, and owner-scoped unique constraints.
  - Extend existing `routes/recommendations.js` scheduling/listing/cancellation controls for media where compatible; shared save/attach uses the unified library contract, not duplicate routes in `conversations.js`. Check ownership of every included thread, source, batch, and library item. Media scope includes only the active Room thread and its non-deleted Side Talks, alongside saved media interests; never accept arbitrary source-thread IDs without validating the relationship. Existing Papers/Apps all-conversation scope remains unchanged.
  - Extend the existing Room/Side Talk composer, draft storage, message routes, `sendMessage`, transcript formatting, and `buildTurnPrompt` to carry typed media references. Reuse the current attachment-chip idiom rather than sending media references down the screenshot vision path or pretending they are full uploaded files. Inspect `studioEmbed`, `wireEmbed`, `fillEmbed`, `saveDraft`, and existing message metadata before choosing the smallest compatible representation.
  - Register bounded read tools alongside the existing interest tools in `conversations.js`, and give tool-less chat models a bounded relevant subset through `buildTurnPrompt`. Every chat model sees the same persisted recommendations and evidence; selecting another chat model never erases the library or restarts discovery.
  - Reuse the existing AI settings and provider/cost controls for background generation. Do not silently replace the selected model for a normal chat reply, buy new services, or introduce uncapped background calls. Cache by source revision, scope, library revision, steering, and feedback; never regenerate simply because a panel opens.
  - Before implementing external adapters, verify currently supported discovery/metadata sources, credentials, and access terms. Google Books is an existing integration to inspect, not an assumption that it can provide every book's content. Do not assume IMDb scraping or a film/TV API account exists. If required access is absent, keep that adapter disabled and say what is missing; never fabricate verification or silently substitute an unrelated service.
  - Use bounded jobs with cancellation, timeouts, retry limits, and restart recovery. An unavailable source or model leaves ordinary Room chats working and preserves the previous batch. Untrusted source text is reference data, never instructions to tools or settings.
  - Frontend work stays in `fmcns_navigator.html`, mirrored to `queue-server/public/index.html`. Any new generated user-facing text carries the shared style; progress and failure messages remain short and plain.
- Completion criteria for the later implementation, following the repository's verification/shipping rules:
  - A preference expressed only in a Side Talk can influence a relevant pick; an unadopted assistant suggestion cannot become a preference by itself.
  - A taste-only recommendation can be selected without a manufactured structural claim. A structural pick can cross settings and scales without relying on shared topic words.
  - Cards explain the positive connection without a default comparison-limit section, forced challenge, unsupported scene claims, or spoilers.
  - Imported/read/watched works do not appear as unseen discoveries; a resurfaced saved work is clearly separate. Save is visible on every recommendation, persists into the combined browsable library, updates to Saved, and remains idempotent through retries/concurrent clicks. An unsaved displayed recommendation creates no library entry or preference evidence.
  - Attach several works from the library or unsaved recommendations, remove one, switch threads/models, refresh, and send: only the intended references reach the intended conversation. The draft survives a failed send; unauthorized or missing IDs cannot be attached; attachments alone do not save recommendations or imply access to the complete works.
  - Scope/timing choices are respected. Changes to preferences, rewinds, deletions, and dismissals invalidate relevant stale evidence. Switching chat models preserves access.
  - Unavailable sources, missing keys, contradictory metadata, quota limits, cancellation, and restart recovery do not freeze chats or generate fake recommendations. Owner isolation holds for every route and tool.
  - The pane remains usable with the sidebar expanded, resized, collapsed/reopened, and on a narrow window; no extra permanent header band is introduced.
- Open implementation choices: exact film/TV discovery adapter and metadata access; whether existing corpus evidence is sufficient for each structural candidate. These require inspection during implementation, not a demand that Antoine select technical providers now. No external accounts, purchases, implementation, or queue dispatch are authorized by this planning request.
